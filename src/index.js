// Jungle Market — Expo entry point
// Registers the root React Native component with Expo's runtime.
import { registerRootComponent } from 'expo';
import App from './App';

registerRootComponent(App);
