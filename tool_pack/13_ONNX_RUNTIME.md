# ONNX Runtime

> **Purpose:** CPU-first inference runtime.

## Use for
- rembg inference
- exported category/material models when validated

## Requirements
- model path/provider configurable
- session reuse
- output validated through adapters

## Do not
- assume ONNX export preserves accuracy without evaluation
